# Сквозной end-to-end процесс Best Ori Gin

## Назначение
Документ описывает продуктовый путь платформы Best Ori Gin по реализованному набору фич, включая employee-контур feature #19, операторскую историю заказов feature #20, employee-обработку претензий feature #21, карточку партнера с отчетами feature #22, настройки сотрудника с повышенными правами feature #23, сквозную аутентификацию с ролями, active partner и имперсонацией feature #24, платформенные уведомления, offline handling, i18n и аналитику feature #25, админский RBAC и управление внутренними учетными записями feature #26, admin CMS для контента, новостей, FAQ и документов feature #27, admin referral funnels feature #28, admin PIM каталог, товары, категории и медиа feature #29, admin catalog campaigns и PDF-выпуски feature #30, а также admin pricing/promotions для цен, акций, shopping offers, подарков и бенефитов feature #31, а также admin WMS/1C для остатков, складов и поставок feature #32. Это не процесс разработки и не workflow артефактов, а пользовательский и операционный сценарий самой платформы.

## Участники
- Гость, покупатель, партнер, спонсор и партнер-лидер.
- Сотрудник call-center/backoffice, сотрудник поддержки, сотрудник с расширенными правами и супервайзер.
- Региональный менеджер и владелец или оператор партнерского офиса.
- Суперадмин, администратор безопасности, HR/операционный администратор, администратор контента, каталога, pricing/promotions, заказов, WMS, платежей, пользователей, ролей и аудита.
- Маркетинг-аналитик, администратор трекинга и владелец локализации.
- Внешние системы WMS/1C, сборки, доставки, оплаты, бонусов, документов и файлового хранения S3/MinIO.

## Основной поток
Пользователь начинает с публичных страниц: главная, новости, FAQ, информационные разделы, документы, benefits и цифровой каталог. Frontend shell инициализирует i18n dictionaries, notification root, offline monitor и consent-aware analytics layer до выполнения пользовательских действий. AuthProvider восстанавливает session context, роль, invitation code, active partner и признак имперсонации. Если маршрут публичный, гость остается в публичном контуре, а invitation code сохраняется до регистрации или входа. Если маршрут закрытый, role router допускает пользователя только в customer, partner, employee или admin-контур, соответствующий его роли и route policy.

После регистрации или активации пользователь получает профиль с контактами, адресами, документами и настройками. Далее пользователь открывает каталог, карточки товаров, промо и корзину, выбирает товары текущей кампании и переходит к оформлению заказа. Каталог, карточка товара, корзина и checkout используют только опубликованные price lists, promo prices, segment rules, gifts и shopping offers текущей кампании, чтобы цена на витрине, в корзине и при подтверждении заказа совпадала.

При оформлении система проверяет остатки, лимиты, кампанию, доставку, оплату, бонусный кошелек и допустимость предложений. Если соединение потеряно, пользователь видит локализованный offline popup, введенные данные остаются видимыми, а неидемпотентные операции не повторяются автоматически без backend idempotency key. После reconnect платформа проверяет session context и показывает локализованное уведомление о восстановлении.

Успешный заказ передается в оплату, WMS, сборку и доставку. Пользователь видит историю заказов, детали, timeline, оплату, доставку, повтор заказа и возможность претензии. Партнер дополнительно видит офлайн-заказы клиентов, отчеты, комиссии, документы, бонусы, MLM-структуру, рост и поставки партнерского офиса. Pageview и conversion события отправляются в Yandex Metrika, Mindbox и Hybrid pixel только через централизованный analytics layer и только по разрешенным consent categories.

## Employee-контур feature #19 и #20
Если покупатель или партнер обращается в поддержку, сотрудник открывает `/employee`, ищет клиента, партнера или заказ, выбирает причину обращения и работает в контролируемом employee-контексте. Для нового телефонного или backoffice-заказа сотрудник открывает `/employee/new-order`, собирает заказ от имени пользователя, выбирает доставку и оплату, подтверждает заказ и получает результат с audit trail.

Feature #20 добавляет операторскую историю заказов как обязательную точку расследования и поддержки. Сотрудник открывает `/employee/order-history`, фильтрует заказы по партнеру, клиенту, периоду, статусам оплаты, доставки и сборки, а также по проблемным признакам. В списке видны только разрешенные и маскированные данные клиента, статусы, суммы, кампания и доступные переходы. При открытии `/employee/order-history/:orderId` сотрудник получает расширенную карточку заказа: состав, totals, промо, бонусы, payment events, delivery tracking, WMS-события, support cases, claims и audit trail.

Из истории и деталей заказа сотрудник переходит в `/employee/order-support`, связанную претензию, платежное событие или карточку партнера без потери orderId, customerId, partnerId и reason-контекста. Супервайзер использует тот же контур для контроля проблемных заказов, ручных вмешательств, SLA и действий сотрудников. Все просмотры списка, открытия карточек и переходы сохраняют audit-события и не попадают в публичный клиентский timeline.

## Employee-контур претензий feature #21
Если обращение требует оформления или обработки претензии, сотрудник открывает `/employee/submit-claim`, находит клиента, партнера или заказ, указывает основание обращения, выбирает позиции, количество, тип проблемы, ожидаемое решение и прикладывает подтверждающие материалы. Система проверяет доступность претензии, лимиты роли, статус заказа, оплату, складской контекст, влияние на бонусы и рассчитывает preview компенсации, SLA и доступные действия. Повторная отправка с тем же idempotency key не создает дубль кейса.

Операционная очередь претензий доступна на `/employee/claims-history`. Сотрудник фильтрует кейсы по статусу, периоду, SLA, ответственному, типу решения, источнику, складу, финансовому статусу и поисковой строке. В списке отображаются только разрешенные и маскированные данные клиента или партнера, номер заказа, сумма компенсации, ответственный, дедлайн SLA и machine-readable действия.

Карточка `/employee/claims-history/:claimId` объединяет состав претензии, вложения, публичные и внутренние комментарии, расчет и утверждение компенсации, маршрут согласования, складскую задачу, финансовую задачу, задачу customer support, timeline статусов и audit trail. Сотрудник передает кейс в складскую проверку, финансы или customer support через допустимые transition-коды. Супервайзер утверждает компенсации выше лимита сотрудника, переназначает ответственных и контролирует просрочки. Итог решения передается пользователю или партнеру как структурированный статус, next action и mnemonic-код `STR_MNEMO_*`, который frontend локализует.

## Employee-контур карточки партнера и отчетов feature #22
Feature #22 добавляет сотруднику и региональному менеджеру точку входа `/employee/partner-card`. Сотрудник ищет партнера по person number, имени, телефону, email или региональному признаку, а система проверяет роль, региональный scope и основание просмотра. Карточка показывает идентификацию партнера, маскированные контакты, статус активности, уровень, наставника, регион, последние заказы, KPI, бонусный баланс, открытые претензии, просроченные действия и риск-сигналы.

Из карточки сотрудник переходит в `/employee/report/order-history`, order support, claim, бонусный кошелек или связанные order details без повторного ввода partnerId, orderNumber и supportReasonCode. Отчет истории заказов партнера агрегирует заказы по периоду, каталогу, статусам оплаты, доставки, сборки и проблемным признакам. Региональный менеджер использует отчет для контроля партнерских KPI, падения активности, открытых претензий, задержек доставки и влияния на персональный или групповой объем.

Все просмотры карточки, отчета и переходы в связанные flows фиксируются в audit trail. Backend возвращает структурированные данные и mnemonic-коды `STR_MNEMO_*`, а frontend локализует UI через i18n dictionaries.

## Employee-настройки и повышенные права feature #23
Feature #23 добавляет внутреннему пользователю маршруты `/employee/profile-settings`, `/employee/profile-settings/general`, `/employee/profile-settings/contacts`, `/employee/profile-settings/addresses`, `/employee/profile-settings/documents`, `/employee/profile-settings/security` и `/employee/super-user`. Сотрудник управляет собственными рабочими реквизитами, контактами, адресами, документами и security-состоянием учетной записи. Эти данные используются employee-потоками поддержки, заказа, претензий, партнерской карточки и отчетности как операционный контекст сотрудника.

Раздел security показывает MFA-статус, активные сессии, security-события, риск-флаги и доступные действия без раскрытия секретов. Раздел документов хранит metadata и fileReferenceId для подтверждающих документов через существующий S3/MinIO слой. Все изменения настроек фиксируются в audit trail и используют версионность, чтобы не терять параллельные обновления.

Маршрут `/employee/super-user` управляет контролируемым elevated mode. Сотрудник выбирает policy, scope, срок действия и основание, а backend проверяет роль, MFA, блокировки, документы и ограничения policy. Если policy требует подтверждения, супервайзер одобряет или отклоняет запрос. Активная elevated session ограничена по времени и scope, передает elevatedSessionId и correlationId в связанные employee flows и может быть завершена сотрудником, отозвана супервайзером или истечь автоматически.

Feature #23 усиливает контроль над impersonation, сервисными операциями, внутренними корректировками, переходами в order, claim, partner, delivery и support flows. Супервайзер и служба безопасности видят историю запросов, одобрений, отказов, отзывов и попыток доступа вне policy. Backend продолжает возвращать только structured data и mnemonic-коды `STR_MNEMO_*`, а frontend локализует все пользовательские тексты через i18n dictionaries.

## Аутентификация, роли и имперсонация feature #24
Feature #24 делает auth context общей точкой входа для публичного, клиентского, партнерского, employee и административного контуров. Login возвращает token, userId, displayName, roles, defaultRoute, route policies, activePartner, invitationCodeState и impersonation state. Current session endpoint восстанавливает эти данные после reload, а logout очищает token, role, activePartner, invitationCode и impersonation context.

Invitation code сохраняется из invite-ссылки, валидируется backend-ом и связывается с пользователем или партнером после login/регистрации. Некорректный код не ломает публичный просмотр, но отображается как локализуемое предупреждение через mnemonic-код. Партнер и партнер-лидер выбирают active partner только из разрешенного scope, а последующие API-запросы получают `X-Active-Partner-Id`.

Сотрудник, супервайзер или администратор запускает имперсонацию только при разрешенной policy, причине, target scope и активной elevated session там, где она требуется. В UI всегда виден баннер активной имперсонации с целевым пользователем, ролью, причиной, сроком и действием завершения. Backend фиксирует `AUTH_IMPERSONATION_STARTED` и `AUTH_IMPERSONATION_FINISHED`, связывая actorUserId, targetUserId, impersonationSessionId, elevatedSessionId и correlationId.

## Уведомления, offline, i18n и аналитика feature #25
Feature #25 добавляет сквозной UX и tracking layer для всех публичных, клиентских, партнерских, employee и admin маршрутов. NotificationProvider показывает success, warning, error, info и blocking-modal сообщения через единый notification root. Все предопределенные тексты берутся из frontend i18n dictionaries или backend mnemonic-кодов `STR_MNEMO_*`; backend не передает hardcoded user-facing текст в API-ответах.

OfflineStatusProvider отслеживает потерю сети и состояние reconnect. При offline state платформа показывает локализованный popup, не скрывает уже введенные данные и блокирует только действия, которым нужна сеть. Checkout, payment, claim, elevated rights, impersonation и другие неидемпотентные операции не повторяются автоматически без idempotency key и подтвержденного backend-состояния. После reconnect система восстанавливает session context и показывает локализованное уведомление.

I18nProvider применяет выбранный язык к notification, modal, offline popup, consent panel, diagnostics и validation feedback без перезагрузки страницы. Владелец локализации управляет `resources_*.ts`, а missing keys диагностируются до production. Новые frontend user-facing строки не хардкодятся в компонентах.

AnalyticsProvider централизует pageview, conversion, notification, offline, consent и error events. Yandex Metrika, Mindbox и Hybrid pixel подключаются через adapters, поэтому бизнес-компоненты не вызывают пиксели напрямую. Consent preferences разделяют functional, analytics и marketing categories. При запрете analytics или marketing соответствующие payload-и не отправляются в запрещенный канал, а пользовательский flow продолжает работать. Администратор трекинга видит diagnostics по каналам, ошибкам adapters и reasonCode `CONSENT_DENIED`, но обычные пользователи не получают доступ к техническим счетчикам.

## Админский RBAC и учетные записи feature #26
Feature #26 добавляет административный контур управления внутренними сотрудниками, служебными учетными записями, ролями, permission sets, зонами ответственности и политиками безопасности. Суперадмин и администратор безопасности открывают admin RBAC workspace, просматривают матрицу доступа и управляют тем, какие employee, partner-office и admin-разделы доступны конкретному сотруднику или служебной интеграции.

HR/операционный администратор создает или обновляет учетную запись сотрудника в разрешенном scope: ФИО, email, телефон, подразделение, должность, статус и дату окончания доступа. Суперадмин или администратор безопасности назначает роли, permission sets и responsibility scopes по региону, складу, пункту выдачи, каталогу, категории товаров, подразделению или сегменту партнерской структуры. Перед сохранением система показывает preview effective permissions, конфликты, requiredMfa и affectedModules, чтобы изменение доступа было осознанным.

Операции с высоким риском требуют MFA или active elevated session: назначение admin-ролей, изменение password/MFA/session policies, создание или ротация service account, emergency deactivation. Backend проверяет permission matrix и scope на каждом admin endpoint-е. Frontend строит доступность действий только по структурированным разрешениям и не использует hardcoded role names. Все пользовательские сообщения приходят как mnemonic-коды `STR_MNEMO_ADMIN_RBAC_*` или берутся из i18n dictionaries.

Служебные учетные записи используются для WMS/1C, сборки, доставки, платежей, бонусной системы и других интеграций. Они имеют owner, integrationType, ограниченные permission scopes, allowedIpRanges и срок действия. Secret показывается только один раз при создании или ротации; далее отображается только masked hint, lastUsedAt и статус. Service account не может выполнять интерактивный login и запускать имперсонацию.

Audit trail фиксирует создание, блокировку и деактивацию учетной записи, назначение роли, изменение permission set, изменение policy, ротацию service account и emergency deactivation. Событие содержит actor, target, actionCode, oldValue, newValue, sourceIp, correlationId и occurredAt, но не содержит пароли, MFA seed, service account secret или session token. Аудитор ищет события по actorUserId, targetUserId, serviceAccountId, roleCode, permissionSetCode, actionCode, периоду и correlationId. Журнал недоступен для редактирования через admin UI.

При инциденте суперадмин выполняет emergency deactivation учетной записи, роли или permission set. Backend немедленно завершает затронутые активные сессии, запрещает дальнейшие запросы с отключенным permission set и сохраняет immutable audit event. Это связывает feature #26 с auth, employee elevated mode, имперсонацией, админскими операциями и интеграциями платформы.

## Админский CMS-контур feature #27
Feature #27 добавляет административный CMS workspace для управления публичным контентом, который потребляют главная, навигация, новости, FAQ, информационные разделы, документы и offer pages. Контент-администратор открывает `/admin/cms`, фильтрует материалы по типу, статусу, языку, аудитории, reviewer-у, publication window и связанным сущностям, затем создает или редактирует материал типа `NEWS`, `ARTICLE`, `CONTENT_PAGE`, `FAQ_ITEM`, `DOCUMENT`, `OFFER_PAGE` или `HOME_BLOCK`.

Редактор собирает материал из структурированных blocks: hero, rich text, image, FAQ group, document link, product link, catalog link, CTA и legal notice. CMS валидирует slug, SEO metadata, обязательные поля, ссылки, вложения, document metadata и publication window до отправки на review. Preview показывает draft render model в desktop и mobile режимах, но не меняет публичную версию.

Материалы проходят editorial или legal review. Юридический reviewer подтверждает документы, правила кампаний, юридические FAQ и offer pages; без такого approval CMS не публикует юридически значимые материалы. Approved version может быть опубликована сразу или переведена в scheduled state. Publication scheduler активирует material version при наступлении publishAt и снимает публикацию при unpublishAt. Публичные feature #001, #002 и #003 читают только published render models, поэтому пользователи не видят draft, rejected, archived или future scheduled content.

Версионность сохраняет каждое содержательное изменение. Rollback создает новую draft version на основе выбранной предыдущей версии, не меняя immutable history. Audit trail фиксирует создание, редактирование, review decision, публикацию, архивирование, rollback, изменение SEO, вложения и schedule с actorUserId, materialId, versionId, oldValue, newValue, comment, sourceRoute, correlationId и occurredAt.

Admin CMS использует permission matrix и responsibility scopes из feature #26. Frontend показывает действия только по разрешениям session context, а backend проверяет доступ на каждом endpoint-е. Все предопределенные сообщения CMS передаются mnemonic-кодами `STR_MNEMO_ADMIN_CMS_*`, а frontend локализует их через `resources_ru.ts` и `resources_en.ts`.

## Админский контур лендингов, инвайтов и рефералов feature #28
Feature #28 добавляет административный workspace для управления партнерскими marketing funnels, которые связывают публичные benefit-лендинги, регистрацию партнеров, sponsor attribution и conversion analytics. Маркетинг-администратор открывает `/admin/referrals`, фильтрует landing variants по типу, статусу, campaign code, локали, owner-у и active window, затем создает или обновляет лендинг для beauty, business или customer referral сценария.

Landing variant состоит из структурированных blocks: hero, benefits, testimonials, CTA registration entrypoint, FAQ, legal notice и campaign metadata. Preview показывает draft render model до публикации, а активация разрешена только при заполненных обязательных блоках, корректном slug, locale, campaign code и непересекающемся active window. Публичные маршруты и sponsor cabinet используют только active landing variant, поэтому гость или кандидат видит актуальные условия и попадает в правильный registration funnel feature #008.

CRM-администратор управляет registration funnels, consent codes, validation rules, invite/referral codes и attribution policy. Referral code может быть одноразовым, многоразовым, персональным или campaign-кодом; он имеет lifecycle `DRAFT`, `ACTIVE`, `USED`, `EXPIRED`, `REVOKED`, `LOCKED`, срок действия, лимит использований, owner partner и ограничения по landing type или региону. Повторная генерация с тем же idempotency key не создает дубль.

Attribution policy задает priority между URL referral code, ручным вводом кода, session context, campaign default sponsor и CRM override. Если источники конфликтуют, система сохраняет выбранный источник, отклоненные источники и reason code. Ручная корректировка sponsor attribution требует отдельного RBAC scope, reason code и комментария; исходная attribution history не удаляется, а событие `ATTRIBUTION_OVERRIDDEN` попадает в audit trail и conversion analytics.

Conversion analytics агрегирует события `LANDING_VIEWED`, `CTA_CLICKED`, `REGISTRATION_STARTED`, `INVITE_CODE_VALIDATED`, `APPLICATION_SUBMITTED`, `CONTACT_CONFIRMED`, `PARTNER_ACTIVATED` и `ATTRIBUTION_OVERRIDDEN` по campaign code, landing variant, funnel, source channel, sponsor и периоду. Отчеты не содержат секретов, одноразовых токенов и лишних персональных данных кандидатов. Все predefined сообщения backend возвращает mnemonic-кодами `STR_MNEMO_ADMIN_REFERRAL_*`, а frontend локализует UI через `resources_ru.ts` и `resources_en.ts`.

## Админский PIM-контур feature #29
Feature #29 добавляет административный PIM workspace `/admin/pim` для управления каталогом косметики, который потребляют публичный каталог feature #004, карточка товара feature #005, цифровые каталоги feature #006, CMS product links feature #27 и будущие кампании/цены feature #030/#031. Категорийный администратор управляет деревом категорий, slug, SEO metadata, parent-child связями, сортировкой, локалью, audience и publication window. Система не допускает циклы в дереве, дубли slug в одном контексте и активацию категории без валидного окна публикации.

PIM-менеджер создает и обновляет товарные карточки: SKU, артикул, бренд, категории, название, описание, состав, инструкции применения, ограничения, атрибуты, теги и storefront visibility. Товар нельзя опубликовать без активной категории, обязательных текстовых полей, главного изображения и разрешенного канала витрины. Публикация создает версию карточки и audit event, а предыдущие версии остаются доступными для расследования и rollback.

Медиа-менеджер загружает и утверждает главное изображение, галерею, PDF-инструкции, сертификаты и вложения с mime type, size, checksum, alt text, locale, usage type и version. Backend отклоняет небезопасные mime type, превышение размера, пустой checksum и конфликт активного checksum. Публичные карточки товаров получают только approved media и published product data.

Коммерческий администратор настраивает cross-sell, alternatives, bundles, recommendations и merchandising blocks с active window и sort order. Блок нельзя активировать с архивным или непубликованным товаром. Import/export jobs позволяют массово загрузить или выгрузить категории, товары, атрибуты и media references; повтор с тем же idempotency key не создает дубли, а row-level validation errors возвращаются структурированно.

Admin PIM использует permission matrix feature #26 и scopes `ADMIN_PIM_VIEW`, `ADMIN_PIM_MANAGE`, `ADMIN_PIM_MEDIA_MANAGE`, `ADMIN_PIM_IMPORT_EXPORT`, `ADMIN_PIM_AUDIT_VIEW`. Backend возвращает предопределенные сообщения только mnemonic-кодами `STR_MNEMO_ADMIN_PIM_*`, а frontend локализует интерфейс через `resources_ru.ts` и `resources_en.ts`. Audit trail фиксирует изменения категорий, товаров, атрибутов, медиа, рекомендаций, import/export jobs и публикаций с actorUserId, entityId, oldValue, newValue, correlationId и occurredAt.

## Админский контур каталогов кампаний и PDF feature #30
Feature #30 добавляет административный workspace `/admin/catalogs` для управления 21-дневными кампаниями косметических каталогов, которые потребляют публичные цифровые каталоги feature #006, документы feature #003, PIM feature #29 и будущие pricing/offer flows. Каталожный менеджер создает кампанию с кодом, названием, локалью, аудиторией, датами действия, freeze window и техническим окном rollover. Кампания связывает текущий, следующий и архивный выпуск, чтобы новый каталог готовился заранее без нарушения действующего выпуска.

Контент-администратор загружает PDF, обложку, изображения страниц и promo tabs. Каждый материал хранит metadata: fileName, mimeType, sizeBytes, checksum, storageKey, version, status и комментарий. Backend отклоняет неподдерживаемый mime type, пустой checksum, конфликт активного checksum и попытку публикации выпуска без approved PDF и page images. Версии материалов сохраняются для расследования и rollback.

Маркетинг-менеджер кампаний расставляет product hotspots на страницах каталога: pageNumber, координаты, размеры, productId, SKU, promoCode, sortOrder и status. Перед публикацией система проверяет hotspots против published PIM-товаров и audience rules. Невалидные координаты, архивный товар или битая ссылка блокируют активацию выпуска и возвращают mnemonic-коды `STR_MNEMO_ADMIN_CATALOG_*`.

Freeze window ограничивает изменения перед переключением каталога. Без emergency override нельзя менять PDF, страницы, hotspots и статус выпуска; отказ фиксируется audit event. Rollover с idempotency key архивирует текущий выпуск, публикует следующий, обновляет current/next/archive flags и передает новый выпуск в публичный цифровой каталог. Повторный rollover с тем же ключом возвращает прежний результат без дублей.

Admin catalog campaigns использует permission matrix feature #26 и scopes `ADMIN_CATALOG_VIEW`, `ADMIN_CATALOG_MANAGE`, `ADMIN_CATALOG_MEDIA_MANAGE`, `ADMIN_CATALOG_ROLLOVER`, `ADMIN_CATALOG_AUDIT_VIEW`. Backend возвращает предопределенные сообщения только mnemonic-кодами `STR_MNEMO_ADMIN_CATALOG_*`, а frontend локализует интерфейс через `resources_ru.ts` и `resources_en.ts`. Audit trail фиксирует изменения кампаний, выпусков, материалов, hotspots, freeze и rollover с actorUserId, entityId, oldValue, newValue, correlationId и occurredAt.

## Админский контур цен, акций, предложений и бенефитов feature #31
Feature #31 добавляет административный workspace `/admin/pricing` для управления коммерческими условиями, которые потребляют публичный каталог, цифровые каталоги, корзина feature #009, checkout feature #010 и партнерские офлайн-заказы feature #017. Pricing manager создает price lists, базовые цены, promo prices и segment rules с привязкой к campaignId, стране, валюте, каналу, роли, партнерскому уровню, региону и active window. Система не допускает дубли priceListCode, пересекающиеся активные периоды цен, валютные конфликты и публикацию цены без published PIM-товара.

Promotions manager создает promotions, shopping offers, bundles, gift rules и threshold benefits. Offer rules описывают eligibility по сумме корзины, SKU, категории, сегменту, роли, partner level и каналу. Compatibility rules управляют priority, stackability, mutually exclusive groups и max benefit, чтобы несколько промо не давали неконтролируемую скидку и не обещали недоступный подарок.

Бизнес-администратор запускает preflight validation перед публикацией: проверяются published PIM-товары, campaign window feature #030, пересечения периодов, min margin threshold, наличие базовых цен для gift SKU и совместимость offers. Валидные price lists и promotions публикуются с idempotency key и становятся доступными каталогу, корзине, checkout и партнерским офлайн-заказам. Emergency pause позволяет немедленно остановить ошибочный offer без удаления истории; корзина и checkout исключают paused offer из расчета.

Import/export jobs позволяют массово загрузить или выгрузить цены, promo prices, segment rules, offers, gifts и threshold benefits. Dry-run возвращает row-level validation summary без изменения бизнес-данных, apply import выполняется идемпотентно и не создает дубли при повторном ключе. Audit trail фиксирует изменения price lists, prices, promo prices, promotions, offers, gifts, imports, exports, publish, pause и archive с actorUserId, entityId, oldValue, newValue, reasonCode, idempotencyKey, correlationId и occurredAt.

Admin pricing/promotions использует permission matrix feature #26 и scopes `ADMIN_PRICING_VIEW`, `ADMIN_PRICING_MANAGE`, `ADMIN_PROMOTION_MANAGE`, `ADMIN_PRICING_PUBLISH`, `ADMIN_PRICING_IMPORT_EXPORT`, `ADMIN_PRICING_AUDIT_VIEW`. Backend возвращает предопределенные сообщения только mnemonic-кодами `STR_MNEMO_ADMIN_PRICING_*`, а frontend локализует интерфейс через `resources_ru.ts` и `resources_en.ts`.
## Админский WMS/1C контур feature #32
Feature #32 добавляет административный workspace `/admin/wms` для управления складской моделью Best Ori Gin. Контур обслуживает публичный каталог, карточку товара, корзину, checkout, партнерские офлайн-заказы, партнерский офис и employee-поддержку заказов. Логистический администратор управляет складами, зонами хранения, каналами продаж, SLA, статусами складов и правилами доступности товара. Складской оператор сопровождает приемку, отгрузку, резервы, ручные корректировки и расхождения. Оператор интеграции WMS/1C управляет источниками обмена, ручными синхронизациями, retry и quarantine.

Остатки отображаются по SKU, складу, catalog period, каналу продаж и статусам sellable, reserved, blocked, inbound и available-to-promise. Availability policy определяет, можно ли продавать товар, резервировать только под существующие заказы, разрешать backorder, удерживать товар на quality hold или полностью скрывать его из продажи. Каталог и карточка товара получают опубликованную доступность без ручного уточнения у склада, а checkout повторно валидирует доступность перед созданием заказа.

Поставки проходят управляемый lifecycle от планирования до приемки. Приемка фиксирует план, факт, недостачу, излишки, повреждения, reason code и внешний документ. Подтвержденная приемка увеличивает остаток один раз, а повторное сообщение WMS/1C с тем же idempotency key не создает повторное движение. Межскладские перемещения и supply-поставки передают данные партнерскому офису, чтобы приемка офлайн-поставок и связанные заказы не расходились со складским контуром.

Интеграция WMS/1C обрабатывает входящие документы идемпотентно по sourceSystem, externalDocumentId, externalLineId и idempotencyKey. Дубли безопасно игнорируются или обновляют существующую запись без повторного изменения остатка. Конфликтные сообщения попадают в quarantine, не меняют sellable stock и требуют решения оператора с reason code. Ошибки обмена, retry, payload checksum, affected SKU и correlationId видны в журнале обмена без раскрытия секретов и лишних персональных данных.

Order workflow использует WMS-контур для резервов, release reservation, частичной отгрузки и backorder possibility. Система не допускает отрицательный sellable stock без явной backorder policy. Если товара недостаточно, frontend получает структурированный результат с доступным количеством, blocked reason и mnemonic-кодом `STR_MNEMO_ADMIN_WMS_*`. Все изменения складов, остатков, поставок, резервов, расхождений и синхронизации фиксируются в audit trail и проверяются RBAC scopes из административного контура.
## Исключения и контроль
Недоступный контент, пустой каталог, невалидная корзина, ошибка оплаты, отсутствие прав, истекшая сессия, route denial, чужой active partner, невалидный invitation code, запрет имперсонации, закрытый заказ, недоступная претензия, невалидный фильтр истории заказов, невалидный фильтр employee claims, невалидные настройки employee-профиля, отказ elevated policy, отсутствие MFA, истекшая elevated session, конфликт RBAC scope, попытка назначить неактивную роль, конфликт permission sets, невалидная security policy, disabled service account, emergency deactivation, невалидный landing variant, конфликт referral code, конфликт attribution policy, невалидный active window, невалидная категория, конфликт SKU, отсутствие главного изображения товара, отказ media validation, конфликт import idempotency, конфликт campaign code, активный freeze window, невалидный hotspot, битая ссылка каталога, конфликт rollover idempotency, конфликт priceListCode, пересечение периодов цен, невалидная promo price, конфликт segment rule priority, недоступный gift SKU, несовместимые offers, конфликт pricing import idempotency, превышение лимита компенсации, конфликт версии, отказ consent, сбой analytics adapter или offline state возвращают mnemonic-коды `STR_MNEMO_*` либо frontend i18n keys, которые frontend локализует из словарей. Backend не передает предопределенные UI-тексты в API. Все действия employee-контура, auth-контура, admin RBAC-контура, admin referral-контура, admin PIM-контура, admin catalog campaigns-контура и admin pricing/promotions-контура и admin WMS/1C-контура сохраняют сотрудника или пользователя, причину, связанные сущности, source route, correlationId, elevatedSessionId, impersonationSessionId и idempotency key там, где операция изменяет состояние. Analytics payload не содержит пароли, токены, полные платежные данные, документы или лишние персональные данные.

## Интеграции
WMS/1C возвращает остатки, поставки, результаты складской проверки и события сборки, система сборки обновляет статусы комплектации, доставка обновляет трекинг и пункты выдачи, платежная система обновляет статус оплаты и возврата, бонусная система начисляет, списывает и корректирует выгоды, S3/MinIO хранит подтверждающие документы, PDF и изображения страниц каталога, административный контур управляет справочниками, кампаниями, ценами, промо, gifts, offers, контентом, лендингами, referral codes, attribution rules, пользователями, ролями, permission scopes, service accounts, правами и аудитом. Yandex Metrika, Mindbox и Hybrid pixel получают только разрешенные consent-aware события через frontend adapters. Операторская история заказов feature #20, employee-претензии feature #21, карточка партнера с отчетами feature #22, employee-настройки с повышенными правами feature #23, auth/имперсонация feature #24, admin RBAC feature #26, admin CMS feature #27, admin referral feature #28, admin PIM feature #29, admin catalog campaigns feature #30 и admin pricing/promotions feature #31 и admin WMS/1C feature #32 агрегируют эти события в контролируемых представлениях без ручного дублирования данных между системами.
