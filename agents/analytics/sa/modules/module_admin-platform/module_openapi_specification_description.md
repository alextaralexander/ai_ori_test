# OpenAPI description feature #36

OpenAPI group `admin-platform` exposes `/v3/api-docs/admin-platform` and `/swagger-ui/admin-platform`. Endpoints return structured DTOs and mnemonic message codes `STR_MNEMO_ADMIN_PLATFORM_*`; frontend resolves user-facing text from i18n dictionaries.