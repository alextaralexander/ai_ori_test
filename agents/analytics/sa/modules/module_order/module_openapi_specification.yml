openapi: 3.0.3
info:
  title: Best Ori Gin module_order API
  version: 2026-04-27
paths:
  /api/order/checkouts: { post: { summary: Start checkout } }
  /api/order/order-history: { get: { summary: Search order history } }
  /api/order/order-history/{orderNumber}: { get: { summary: Get order details } }
  /api/order/order-history/{orderNumber}/repeat: { post: { summary: Repeat order } }
  /api/order/claims: { get: { summary: Search claims }, post: { summary: Create claim } }
  /api/order/claims/{claimId}: { get: { summary: Get claim details } }
  /api/order/claims/{claimId}/comments: { post: { summary: Add claim comment } }
  /api/order/partner-offline-orders: { get: { summary: Search partner offline customer orders } }
  /api/order/partner-offline-orders/{orderNumber}: { get: { summary: Get partner offline order details } }
  /api/order/partner-offline-orders/{orderNumber}/actions: { post: { summary: Execute partner offline order action } }